"""Personal, shared collaboration, and enterprise knowledge memory tiers (Phase 4).

See ``memory_service.create_memory_service`` for the single wiring entry
point (policy + governance + repositories + stores). Contains no agent
reasoning: every business/debugging agent is an independently deployed
Azure AI Foundry agent resource (Phase 3); this package only stores and
governs the content those agents produce.
"""
